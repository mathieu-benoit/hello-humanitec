externals: {}
profile: humanitec/default-module
spec:
  containers:
    shippingservice:
      files: {}
      image: gcr.io/google-samples/microservices-demo/shippingservice:v0.6.0
      liveness_probe:
        command:
          - "/bin/grpc_health_probe\_"
          - '-addr=:50051'
        type: command
      readiness_probe:
        command:
          - /bin/grpc_health_probe
          - '-addr=:50051'
        type: command
      resources:
        limits:
          cpu: '0.250'
          memory: 256Mi
        requests:
          cpu: '0.025'
          memory: 64Mi
      variables:
        DISABLE_PROFILER: '1'
        PORT: '50051'
      volume_mounts: {}
  service:
    ports:
      grpc:
        container_port: 50051
        protocol: TCP
        service_port: 50051
