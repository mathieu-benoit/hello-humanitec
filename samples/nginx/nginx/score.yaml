apiVersion: score.dev/v1b1
metadata:
  name: nginx
containers:
  nginx:
    image: nginx:alpine-slim
resources:
  dns:
    type: dns
  route:
    type: route
    params:
      host: ${resources.dns.host}
      path: /
      port: 80
