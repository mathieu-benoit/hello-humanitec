apiVersion: humanitec.org/v1b1
profile: humanitec/default-module
spec:
  containers:
    recommendationservice:
      liveness_probe:
        port: 8080
        type: grpc
      readiness_probe:
        port: 8080
        type: grpc