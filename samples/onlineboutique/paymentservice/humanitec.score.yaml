apiVersion: humanitec.org/v1b1
profile: humanitec/default-module
spec:
  containers:
    paymentservice:
      liveness_probe:
        grpc:
          port: 50051
        type: grpc
      readiness_probe:
        grpc:
          port: 50051
        type: grpc