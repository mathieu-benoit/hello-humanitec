apiVersion: humanitec.org/v1b1
profile: humanitec/default-module
spec:
  containers:
    emailservice:
      liveness_probe:
        command:
          - /bin/grpc_health_probe
          - '-addr=:8080'
        type: command
      readiness_probe:
        command:
          - /bin/grpc_health_probe
          - '-addr=:8080'
        type: command