apiVersion: humanitec.org/v1b1
profile: humanitec/default-module
spec:
  containers:
    emailservice:
      liveness_probe:
        grpc:
          port: 8080
          type: "grpc"
      readiness_probe:
        grpc:
          port: 8080
          type: "grpc"