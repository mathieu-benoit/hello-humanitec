apiVersion: score.dev/v1b1
metadata:
  name: cartservice
containers:
  adservice:
    image: gcr.io/google-samples/microservices-demo/cartservice:v0.6.0
    variables:
      SPANNER_CONNECTION_STRING: "${resources.redis.name}:6379"
resources:
  redis:
    type: service
    properties:
      name:
service:
  ports:
    grpc:
      port: 7070
      targetPort: 7070